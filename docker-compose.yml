version: '3.7'
services:
  redis:
    image: redis:6.0.9
    ports:
      - 6379:6379
  localstack:
    image: localstack/localstack
    ports:
      - 4566:4566
    environment:
      - SERVICES=sqs
      - DOCKER_HOST=unix:///var/run/docker.sock
    volumes:
      - ./docker:/docker-entrypoint-initaws.d
